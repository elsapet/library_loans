<%= turbo_stream.prepend "all_books", partial: "book", locals: { book: @book } %>

<%= turbo_stream.replace "all_books_counter" do %>
  <h2 id="all_books_counter">All (Available: <%= Book.available.count %>)</h2>
<% end %>